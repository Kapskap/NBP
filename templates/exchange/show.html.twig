{% extends 'base.html.twig' %}
{% block title %}
    | show
{% endblock %}
{% block body %}
    <div class="content">
        Znaleziono <b> {{ exchange|length }} </b> kursy dla waluty
        {{ exchange|last.currency }}.
        <br><br>
    </div>

    <table>
        <tr><td><b>Waluta</b></td><td><b>Kurs</b></td><td><b>Oznaczenie</b></td><td><b>Data</b></td></tr>

        {% for rate in exchange %}
        <tr>
            <td> {{ rate.currency }}  </td>
            <td> {{ rate.mid }} </td>
            <td>{{ rate.code }}</td>
            <td>{{ rate.importat|date("Y-m-d") }}</td>
        </tr>
        {% endfor %}


    </table>

    <br>
    <a href="{{ path('app_browse') }}"><i> Powrót do przeglądania wszystkich walut </i></a>

{% endblock %}
